"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {useRouter} from "next/navigation";
const API_URL=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
export default function MessagesPage(){const router=useRouter();const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");useEffect(()=>{const token=localStorage.getItem("hamnavaz_token");if(!token){router.replace("/auth/login");return;}fetch(`${API_URL}/messages/`,{headers:{Authorization:`Bearer ${token}`},cache:"no-store"}).then(async r=>{if(r.status===401){localStorage.removeItem("hamnavaz_token");router.replace("/auth/login");return []}if(!r.ok)throw new Error();return r.json()}).then(d=>setItems(Array.isArray(d)?d:d.items||d.results||[])).catch(()=>setError("دریافت پیام‌ها ناموفق بود.")).finally(()=>setLoading(false))},[router]);return <main className="container page-shell"><nav className="nav"><Link className="brand" href="/"><span>♪</span> همنواز</Link><div className="nav-links"><Link href="/dashboard">داشبورد</Link><Link href="/musicians">کشف نوازنده‌ها</Link></div></nav><section className="page-head"><p className="eyebrow">ارتباط</p><h1>پیام‌های تو.</h1><p className="lead">درخواست‌های همکاری و گفتگوهای موسیقیایی را از اینجا دنبال کن.</p></section>{error&&<div className="error">{error}</div>}<section className="results-grid">{loading?<div className="empty">در حال دریافت پیام‌ها...</div>:items.length?items.map((m,i)=><article className="panel message-card" key={m.id||i}><div className="name-row"><h2>{m.sender_name||m.sender?.display_name||"همنواز"}</h2>{m.created_at&&<small>{new Date(m.created_at).toLocaleDateString("fa-IR")}</small>}</div><p>{m.content||m.message||m.text||"درخواست همکاری"}</p><span className="verified">پیام</span></article>):<div className="empty">هنوز پیامی نداری. از صفحه نوازنده‌ها یک همکاری شروع کن.</div>}</section></main>}

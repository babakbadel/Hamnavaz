"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
const API_URL=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
export default function InstrumentsPage(){const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{fetch(`${API_URL}/instrument/`).then(r=>r.ok?r.json():[]).then(d=>setItems(Array.isArray(d)?d:d.items||d.results||[])).finally(()=>setLoading(false))},[]);return <main className="container page-shell"><nav className="nav"><Link className="brand" href="/"><span>♪</span> همنواز</Link><div className="nav-links"><Link href="/musicians">نوازنده‌ها</Link><Link href="/dashboard">داشبورد</Link></div></nav><section className="page-head"><p className="eyebrow">کشف ساز</p><h1>ساز مورد علاقه‌ات را پیدا کن.</h1><p className="lead">سازها را ببین و از آن‌ها برای پیدا کردن نوازنده مناسب استفاده کن.</p></section><div className="results-grid">{loading?<div className="empty">در حال بارگذاری سازها...</div>:items.length?items.map((x,i)=><article className="panel" key={x.id||i}><h2>{x.name||x.title||`ساز ${i+1}`}</h2><p>{x.description||"نوازنده‌های این ساز را پیدا کن."}</p><Link className="cta-button" href="/musicians">پیدا کردن نوازنده</Link></article>):<div className="empty">هنوز سازی ثبت نشده است.</div>}</div></main>}
